# Dependencias (no deberia haber, pero por las dudas)
node_modules/

# Logs y temporales
*.log
.DS_Store

# Entorno local (Fase 2: SMTP, tokens). NUNCA commitear secretos.
.env
.env.*
!.env.example

# Cuerpos efimeros de las alertas (los arma el Action en cada corrida).
alerta-cuerpo.txt
telegram-msg.txt
